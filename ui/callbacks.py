"""UI callbacks placeholder for Top‑LoRAs.

This module will eventually hold gallery.select, refresh and generate callbacks
so they can be unit-tested in isolation. At this stage it's an importable
placeholder to support iterative refactoring.
"""

from typing import Any


def placeholder_callback(*args, **kwargs) -> Any:
    return None
