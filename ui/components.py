"""UI components placeholder for Top‑LoRAs.

This module will eventually contain functions to compose Gradio components
and small helpers for consistent component creation. For now it provides
minimal placeholders so the modularization can proceed incrementally.
"""

from typing import Any


def placeholder():
    """Placeholder function to indicate module presence."""
    return None
